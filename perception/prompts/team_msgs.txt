这是《和平精英》屏幕右侧队友通讯区域的裁剪图。
可能有 0 到多条黄色/白色文字消息（如「队友4：我这里有SCAR-L突击步枪！」
「队友2：278' 标记了一处地点」），逐行排列，行数不定。

输出 JSON：
{"lines": ["每条消息完整照抄，从上到下", ...]}
没有消息输出 {"lines": []}。数字和标点照原样，不要规整化。


【换行合并规则】画面里一条消息可能因宽度换行成两行。合并规则：每条消息必须以「我：」或「队友N：」开头；如果某一行不以此开头，它是上一条消息的换行延续，必须并回上一条，不许作为独立行输出。